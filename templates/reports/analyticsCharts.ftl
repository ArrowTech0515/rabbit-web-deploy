<#if analyticsMediumUrl?? && analyticsMediumUrl != ''>
<#include "incBeginPage.ftl"/>
    <img width="100%" src="${analyticsMediumUrl}" style="padding: 10px 0;" />
<#include "incEndPageWithSignature.ftl"/>
</#if>
<#if analyticsSourceUrl?? && analyticsSourceUrl != ''>
<#include "incBeginPage.ftl"/>
    <img width="100%" src="${analyticsSourceUrl}" style="padding: 10px 0;" />
<#include "incEndPageWithSignature.ftl"/>
</#if>
<#if analyticsBrowserUrl?? && analyticsBrowserUrl != ''>
<#include "incBeginPage.ftl"/>
    <img width="100%" src="${analyticsBrowserUrl}" style="padding: 10px 0;" />
<#include "incEndPageWithSignature.ftl"/>
</#if>
<#if analyticsTypeUrl?? && analyticsTypeUrl != ''>
<#include "incBeginPage.ftl"/>
    <img width="100%" src="${analyticsTypeUrl}" style="padding: 10px 0;" />
<#include "incEndPageWithSignature.ftl"/>
</#if>
<#if analyticsMobileUrl?? && analyticsMobileUrl != ''>
<#include "incBeginPage.ftl"/>
    <img width="100%" src="${analyticsMobileUrl}" style="padding: 10px 0;" />
<#include "incEndPageWithSignature.ftl"/>
</#if>
<#if analyticsCountryUrl?? && analyticsCountryUrl != ''>
<#include "incBeginPage.ftl"/>
    <img width="100%" src="${analyticsCountryUrl}" style="padding: 10px 0;" />
<#include "incEndPageWithSignature.ftl"/>
</#if>
